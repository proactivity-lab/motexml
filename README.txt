
Mote XML Library.

 * Generate default dt_types.h with "make types" in the lib directory.
   For custom types see python/gentypes.py --help

 * Build the library with "make library" in the lib directory.

 * Do both by simply typing make in the lib directory.
